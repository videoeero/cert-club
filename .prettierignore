dist
node_modules
package-lock.json

# Question bank content and test fixtures: hand-authored/generated data
# files with their own formatting conventions, not app source code.
certs/
test/fixtures/

# Hand-formatted tables; reformat separately and deliberately rather than
# picking this up in routine `format` runs.
CONTRIBUTING.md
